# Default locations consumed by Projeto.py when no flag is provided.
# Each section adds arguments for the respective subcommand.
embed:
  data_dir: ./archive
  csv_path: ./classificacao.csv
  out_dir: ./outputs/embeddings_resnet50

train-density:
  csv: ./classificacao.csv
  dicom_root: ./archive
  outdir: ./outputs/mammo_efficientnetb0_density
