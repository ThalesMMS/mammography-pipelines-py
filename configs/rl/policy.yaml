# Placeholder policy configuration consumed by Projeto.py rl-refine.
algorithm: random  # options: random, threshold, ppo, a2c
episodes: 5
window: 4
reward_scale: 1.0
total_timesteps: 5000
learning_rate: 0.0003
gamma: 0.99
exploration:
  epsilon_start: 0.3
  epsilon_final: 0.05
  decay_steps: 5000
