# Legacy install path. Prefer "uv sync --frozen" with uv.lock for reproducibility.
# Note: PyTorch 2.10+ drops macOS x86_64 wheels. Intel Mac users should pin an
# older torch version or switch to Apple Silicon.
-r requirements/requirements-base.txt
torch==2.10.0
torchvision==0.25.0
